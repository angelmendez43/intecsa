<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

      <record id="intecsa_view_order_form" model="ir.ui.view">
          <field name="name">intecsa.order.form</field>
          <field name="model">sale.order</field>
          <field name="inherit_id" ref="sale.view_order_form" />
          <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="entrega_id" string="Entrega"/>
                <field name="comentarios" string="Comentarios"/>
                <field name="oferta_venta" string="Ofertas de ventas"/>
            </field>
          </field>
      </record>

    </data>
</odoo>
